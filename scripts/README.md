Utility scripts, not part of the published packages.

  check-fixture.mjs   print the pipeline fixture's per-word acoustics and the
                      typography they resolve to — the quickest way to eyeball
                      whether an analysis run looks sane.
